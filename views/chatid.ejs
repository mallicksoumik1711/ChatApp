<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat ID Generation</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col items-center p-4">

    <!-- Navbar -->
    <nav class="w-full bg-white flex justify-between items-center px-6 py-4 shadow-md fixed top-0 left-0">
        <div class="text-xl font-bold text-gray-700 flex items-center">
            <img src="/images/logo.png" alt="Logo" class="h-8 mr-2">
            Whispr
        </div>
        <a href="/landing-page" class="bg-red-500 hover:bg-red-600 text-white px-8 py-2 rounded-lg transition">Exit</a>
    </nav>

    <!-- Spacer to prevent content from being hidden behind fixed navbar -->
    <div class="h-16"></div>

    <!-- Welcome Message -->
    <div class="text-center text-lg sm:text-xl md:text-2xl mt-6 mb-6">
        <p class="text-gray-700">Welcome, <span class="text-red-600 font-semibold">
                <%= user.username %>
            </span></p>
        <p class="text-gray-500 text-sm sm:text-base">Generate your chat ID here.</p>
        <p class="text-gray-500 text-sm sm:text-base">Any length of chatid is accepted, generate your own chatid and share it with your freinds to chat with them.</p>
    </div>

    <!-- Chat ID Container -->
    <div class="w-full max-w-sm sm:max-w-md md:max-w-lg bg-white shadow-lg rounded-lg overflow-hidden flex flex-col">
        <!-- Chat Header -->
        <div
            class="bg-gradient-to-r from-blue-500 to-blue-700 text-white text-lg sm:text-xl font-bold text-center py-4">
            Enter Chat ID
        </div>

        <!-- Input Box -->
        <div class="p-6">
            <form action="/chat-id-form" method="post" class="space-y-4">
                <input type="text" name="chatid"
                    class="w-full p-3 border rounded-lg outline-none text-gray-700 text-sm sm:text-base"
                    placeholder="Enter your ID here">
                <input type="submit" value="Generate Chat"
                    class="w-full p-3 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-lg cursor-pointer transition text-sm sm:text-base">
            </form>
        </div>
    </div>

</body>

</html>